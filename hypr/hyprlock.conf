# $hyprDir = $HOME/.config/hypr
$mpris_art = /tmp/hyde-mpris.png
$wall = $HOME/.config/hypr/photo_2025-09-29_19-05-02.jpg

# ===== ЦВЕТА ДЛЯ БЫСТРОЙ НАСТРОЙКИ =====
# Основные цвета
$bg_color = rgba(192032)           # основной фон
$fg_color = rgba(D3ECF2)           # основной текст
$accent_color = rgba(297C94)       # акцентный цвет
$base_color = rgba(0E0D1E)         # базовый цвет
$active_border_color = rgba(AB2F64) # цвет активной границы
$inactive_border_color = rgba(BB582E) # цвет неактивной границы
$shadow_color = rgba(040405ee)       # цвет тени

# Основные цвета текста
$color_primary = rgb(D3ECF2)              # 🌊 СВЕТЛО-ГОЛУБОЙ - Главный текст
$color_secondary = rgb(B78DB9)            # 🌸 СИРЕНЕВО-РОЗОВЫЙ - Второстепенный текст
$color_tertiary = rgb(674B60)             # 🍇 ТЕМНО-ФИОЛЕТОВЫЙ - Дополнительный текст
$color_date = rgba(4B4C4A, .75)           # 🪶 СЕРЫЙ - Дата

# Цвета элементов интерфейса
$color_bg_elements = rgba(363B5D, 0.6)    # 🌑 ТЕМНО-СИНИЙ - Фон элементов
$color_border_avatar = rgba(297C94, .75)  # 🔵 СИНЕ-ЗЕЛЕНЫЙ - Граница аватарки
$color_border_album = rgba(827898, 0.8)   # 🟣 СЕРЫЙ-ФИОЛЕТОВЫЙ - Граница обложки
$color_input_bg = rgba(040405, 0.9)       # ⚫ ОЧЕНЬ ТЕМНЫЙ - Фон поля ввода
$color_shape_bg = rgba(042C24, 0.7)       # 🌿 ТЕМНО-ЗЕЛЕНЫЙ - Фон фигур
# ===== КОНЕЦ НАСТРОЙКИ ЦВЕТОВ =====

# BACKGROUND
background {
    monitor =
    path = $wall
}

# GENERAL
general {
    no_fade_in = false
    disable_loading_bar = false
    grace = 1
}

# GREETINGS
label {
    monitor =
    text = Welcome!
    color = $color_primary       # 🌊 СВЕТЛО-ГОЛУБОЙ - Текст приветствия
    font_size = 55
    font_family = JetBrainsMono Nerd Font
    position = -630, 320
    halign = center
    valign = center
}

# Time
label {
    monitor =
    text = cmd[update:1000] echo "<span>$(date +"%I:%M")</span>"
    color = $color_primary       # 🌊 СВЕТЛО-ГОЛУБОЙ - Время
    font_size = 40
    font_family = JetBrainsMono Nerd Font
    position = -630, 240
    halign = center
    valign = center
}

# Day-Month-Date
label {
    monitor =
    text = Sunday, September 29
    color = $color_date          # 🪶 СЕРЫЙ - Дата
    font_size = 20
    text_align = left
    font_family = JetBrainsMono Nerd Font
    position = 180, 175
    halign = left
    valign = center
}

# Profie-Photo
image {
    monitor =
    path = ~/.config/hypr/pfp.webp
    border_size = 1
    border_color = $color_border_avatar  # 🔵 СИНЕ-ЗЕЛЕНЫЙ - Граница аватарки
    size = 160
    rounding = -1
    rotate = 0
    reload_time = -1
    reload_cmd =
    position = -630, 25
    halign = center
    valign = center
}

# USER-BOX
shape {
    monitor =
    size = 320, 55
    color = $color_shape_bg      # 🌿 ТЕМНО-ЗЕЛЕНЫЙ - Фон поля пользователя
    rounding = -1
    border_size = 0
    border_color = $inactive_border_color
    rotate = 0
    xray = false
    position = -630, -140
    halign = center
    valign = center
}

# USER
label {
    monitor =
    text =   $USER
    color = $color_secondary     # 🌸 СИРЕНЕВО-РОЗОВЫЙ - Текст имени пользователя
    outline_thickness = 0
    dots_size = 0.2
    dots_spacing = 0.2
    dots_center = true
    font_size = 16
    font_family = JetBrainsMono Nerd Font
    position = -630, -140
    halign = center
    valign = center
}

# INPUT FIELD
input-field {
    monitor =
    size = 320, 55
    outline_thickness = 0
    dots_size = 0.2
    dots_spacing = 0.2
    dots_center = true
    outer_color = rgba(255, 255, 255, 0)
    inner_color = $color_input_bg        # 🌑 ТЕМНО-СИНИЙ - Фон поля ввода
    font_color = $color_primary          # 🌊 СВЕТЛО-ГОЛУБОЙ - Текст ввода пароля
    fade_on_empty = false
    font_family = JetBrainsMono Nerd Font
    placeholder_text = <i><span foreground="##B78DB9"> Enter Pass</span></i>
    hide_input = false
    position = -630, -220
    halign = center
    valign = center
}

# Music blur div
shape {
    monitor =
    size = 350, 110
    color = $color_shape_bg      # 🌿 ТЕМНО-ЗЕЛЕНЫЙ - Фон музыкального виджета
    rounding = 20
    rotate = 0
    position = -630, -350
    halign = center
    valign = center
    zindex = 1
}

# Music Album Art
image {
    monitor =
    path = $mpris_art
    size = 86
    border_size = 3
    border_color = $color_border_album  # 🟣 СЕРЫЙ-ФИОЛЕТОВЫЙ - Граница обложки альбома
    rounding = 5
    rotate = 0
    reload_time = 0
    reload_cmd =
    position = 167, -350
    halign = left
    valign = center
    zindex = 5
}

# PLAYER ARTIST
label {
    monitor =
    text = cmd[update:1000] echo "$($music --artist)"
    color = $color_secondary     # 🌸 СИРЕНЕВО-РОЗОВЫЙ - Исполнитель трека
    font_size = 12
    font_family = JetBrains Mono Nerd Font Mono ExtraBold
    position = 270, -375
    halign = left
    valign = center
    zindex = 5
}

# PLAYER TITLE
label {
    monitor =
    text = cmd[update:1000] echo "$($music --title)"
    color = $color_primary       # 🌊 СВЕТЛО-ГОЛУБОЙ - Название трека
    font_size = 14
    font_family = JetBrains Mono Nerd Font Mono ExtraBold
    position = 270, -350
    halign = left
    valign = center
    zindex = 5
}

# PLAYER SOURCE
label {
    monitor =
    text = cmd[update:1000] echo "$($music --source)" &
    color = $color_tertiary      # 🍇 ТЕМНО-ФИОЛЕТОВЫЙ - Источник музыки
    font_size = 12
    font_family = JetBrains Mono Nerd Font Mono
    position = 270, -320
    halign = left
    valign = center
    zindex = 5
}